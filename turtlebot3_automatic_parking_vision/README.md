# TurtleBot3 Automatic Parking - AR Marker detection via Vision Processing version

This needs:

``` bash
$ cd catkin_ws/src/
$ git clone https://github.com/ar-tools/ar_tools.git
$ cd catkin_ws/ && catkin_make
```